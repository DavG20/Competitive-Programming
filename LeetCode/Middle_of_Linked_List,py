#Definition for singly-linked list.
class ListNode(object):
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next
class Solution(object):
    def middleNode(self, head):
        """
        :type head: ListNode
        :rtype: ListNode
        """
        newNode=ListNode(0)
        print(newNode,"node")
        count=0
        while newNode:
            count+=1
            newNode=newNode.next
        print(count)
c=Solution()
c.middleNode([1,2,3,4,5,6])